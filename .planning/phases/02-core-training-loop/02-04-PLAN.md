---
phase: 02-core-training-loop
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/features/workouts/components/workout-history-list.tsx
  - src/features/workouts/components/workout-history-card.tsx
  - src/features/dashboard/components/client-list-table.tsx
  - src/features/dashboard/components/client-status-badge.tsx
  - src/features/dashboard/components/client-search.tsx
  - src/pages/client/workout-history.tsx
  - src/pages/trainer/TrainerDashboard.tsx
  - src/pages/trainer/client-detail.tsx
  - src/App.tsx
  - public/locales/en/translation.json
  - public/locales/es/translation.json
autonomous: false
requirements:
  - WLOG-04
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Client can view their workout history showing past sessions with all logged sets"
    - "Each history entry shows session date, training day, duration, and total sets"
    - "Trainer can view a scrollable list of all clients with activity status"
    - "Trainer can search clients by name from the dashboard"
    - "Trainer sees color-coded indicators for today's workout completion (green/yellow/gray)"
    - "Trainer can tap a client to drill into their plan and logs"
    - "All dashboard and history UI is fully translated in Spanish and English"
  artifacts:
    - path: "src/pages/client/workout-history.tsx"
      provides: "Client workout history page"
      contains: "WorkoutHistoryPage"
    - path: "src/features/dashboard/components/client-list-table.tsx"
      provides: "Trainer client list with status indicators"
      contains: "ClientListTable"
    - path: "src/features/dashboard/components/client-status-badge.tsx"
      provides: "Color-coded status badges"
      contains: "ClientStatusBadge"
    - path: "src/pages/trainer/TrainerDashboard.tsx"
      provides: "Upgraded trainer dashboard with client status"
      contains: "TrainerDashboard"
  key_links:
    - from: "src/pages/client/workout-history.tsx"
      to: "src/features/workouts/hooks/useWorkouts.ts"
      via: "useWorkoutHistory hook"
      pattern: "useWorkoutHistory"
    - from: "src/features/dashboard/components/client-list-table.tsx"
      to: "src/features/dashboard/hooks/useDashboard.ts"
      via: "useClientDashboard RPC hook"
      pattern: "useClientDashboard"
    - from: "src/features/dashboard/components/client-list-table.tsx"
      to: "src/pages/trainer/client-detail.tsx"
      via: "Navigation on row click"
      pattern: "navigate.*clients.*clientId"
---

<objective>
Build the client workout history page and upgrade the trainer dashboard with a searchable client list, color-coded workout completion indicators, and client detail drill-in. This completes the feedback loop: clients see their progress, and the trainer monitors all clients at a glance.

Purpose: Workout history gives clients visibility into their training consistency and logged progress. The trainer dashboard is Javier's daily tool for monitoring his 20-50 clients -- he needs to quickly see who has trained today, who hasn't, and drill into any client's details. This plan completes all Phase 2 features.

Output: Client workout history page, upgraded trainer dashboard with client status, client detail page with workout tabs, human verification of the complete Phase 2 flow.
</objective>

<execution_context>
@/Users/macbookpro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookpro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-training-loop/02-RESEARCH.md
@.planning/phases/02-core-training-loop/02-CONTEXT.md
@.planning/phases/02-core-training-loop/02-01-SUMMARY.md
@.planning/phases/02-core-training-loop/02-02-SUMMARY.md
@.planning/phases/02-core-training-loop/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build client workout history and upgrade trainer dashboard with client status</name>
  <files>
    src/features/workouts/components/workout-history-list.tsx
    src/features/workouts/components/workout-history-card.tsx
    src/features/dashboard/components/client-list-table.tsx
    src/features/dashboard/components/client-status-badge.tsx
    src/features/dashboard/components/client-search.tsx
    src/pages/client/workout-history.tsx
    src/pages/trainer/TrainerDashboard.tsx
    src/pages/trainer/client-detail.tsx
    src/App.tsx
    public/locales/en/translation.json
    public/locales/es/translation.json
  </files>
  <action>
    **Claude's Discretion decisions (from CONTEXT.md):**
    - Workout history: simple date list (simplest useful approach -- no calendar view for v1)
    - Dashboard home: client list first (for 20-50 clients, the list IS the dashboard -- no need for a separate summary page)
    - Status colors: green = completed today, yellow = in progress, gray = not started today. Clean and conventional scheme.

    **CLIENT WORKOUT HISTORY:**

    1. Create `src/features/workouts/components/workout-history-card.tsx`:
       - Props: `entry: WorkoutHistoryEntry`
       - Card showing: date (formatted with date-fns), training day label, session duration (formatted with formatSessionDuration), total sets logged, training day name
       - Compact card layout for scrollable list
       - Date in bold, other info as secondary text
       - If session was not completed (completed_at is null), show "Incomplete" badge in yellow

    2. Create `src/features/workouts/components/workout-history-list.tsx`:
       - Props: `clientId: string`
       - Uses `useWorkoutHistory(clientId)` hook
       - Renders WorkoutHistoryCard for each session
       - Groups by month (optional -- if simple, just a flat list sorted by date desc)
       - Loading state: skeleton cards
       - Empty state: "No workouts yet. Start your first workout!" with dumbbell icon
       - Error state with retry
       - Scrollable, default last 30 days of history

    3. Create `src/pages/client/workout-history.tsx`:
       - Route: `/client/history`
       - Page title: "Workout History" (translated)
       - Uses `useAuth()` for client ID
       - Renders WorkoutHistoryList with client's own ID
       - All text translated

    **TRAINER DASHBOARD UPGRADE:**

    4. Create `src/features/dashboard/components/client-status-badge.tsx`:
       - Props: `status: 'active' | 'inactive' | 'pending'`, `todayStatus: 'completed' | 'in_progress' | 'not_started' | null`
       - Activity status badge: "Active" (green text), "Inactive" (gray text), "Pending" (yellow text)
       - Today's workout indicator (separate from activity status): green circle = completed, yellow circle = in progress, gray circle = not started, no circle if no active plan
       - Uses consistent color scheme from app's design tokens (not hardcoded hex values)
       - Small, compact badges that fit in a table row

    5. Create `src/features/dashboard/components/client-search.tsx`:
       - Search input (Input component) with Search icon (lucide)
       - Props: `value: string`, `onChange: (value: string) => void`
       - Full-width on mobile, debounced input (300ms)
       - Placeholder: "Search clients..." (translated)
       - Clear button (X icon) when search has value

    6. Create `src/features/dashboard/components/client-list-table.tsx`:
       - Props: `clients: ClientDashboardEntry[]`, `searchQuery: string`
       - Filters clients by searchQuery (case-insensitive name match)
       - Renders each client as a row/card (use cards on mobile, not a table -- better touch targets):
         - Client name (bold)
         - Activity status badge (ClientStatusBadge)
         - Today's workout indicator (green/yellow/gray dot)
         - "Has plan" indicator (checkmark or dash)
         - Last workout date (relative: "Today", "2 days ago", "1 week ago" using date-fns formatDistanceToNow)
       - Each row/card is tappable -- navigates to `/trainer/clients/:clientId` (client detail)
       - Sort: clients with today's incomplete/not-started workouts first, then by name
       - Empty state if no clients match search
       - Mobile: stack as cards with full-width, 56px min height per card for touch targets

    7. Rewrite `src/pages/trainer/TrainerDashboard.tsx`:
       - Replace the existing Phase 1 dashboard (which was just a client activation list)
       - Page title: "Dashboard" (translated)
       - ClientSearch component at top
       - ClientListTable below with data from `useClientDashboard()` hook
       - Keep the client activation functionality: the Activate/Deactivate buttons should still work (DASH-01 includes activity status management). Integrate activation toggle into the client list or client detail page.
       - Loading state, error state with retry
       - Shows total client count: "{n} clients" in header area
       - **NOTE:** The existing TrainerDashboard.tsx has a client activation feature using TanStack Query. Preserve this functionality -- either integrate it into the new client list (inline activate/deactivate button per client) or move it to the client detail page. Do NOT break the Phase 1 AUTH-03 requirement.

    8. Update `src/pages/trainer/client-detail.tsx` (created as placeholder in Plan 02):
       - Populate the "Workouts" tab (Tab 2) with workout history for the selected client
       - Use WorkoutHistoryList component with the client's ID
       - Tab 1 "Plan" should show the client's active plan (inline or link to plan view)
       - Add Activate/Deactivate toggle in the client detail header if not handled in dashboard
       - Trainer can see everything the client sees about their own data

    9. Update `src/App.tsx`:
       - Ensure `/client/history` route points to WorkoutHistoryPage
       - All routes from Plans 02 and 03 should already exist; verify they're present

    10. Update translation files (both en and es):
        - History: "Workout History", "No workouts yet", "Start your first workout", "Duration", "Sets", "Incomplete", "{date} ago"
        - Dashboard: "Dashboard", "Search clients...", "clients", "Active", "Inactive", "Pending", "Has Plan", "No Plan", "Last Workout", "Today", "Never", "Activate", "Deactivate"
        - Status: "Completed", "In Progress", "Not Started"
        - Ensure ALL keys exist in BOTH en and es files
  </action>
  <verify>
    Verify `pnpm build` passes with zero TypeScript errors.
    Verify `src/pages/client/workout-history.tsx` uses useWorkoutHistory hook.
    Verify `src/features/dashboard/components/client-status-badge.tsx` handles green/yellow/gray colors for today_workout_status.
    Verify `src/features/dashboard/components/client-list-table.tsx` filters by search query and navigates to client detail on tap.
    Verify `src/pages/trainer/TrainerDashboard.tsx` uses useClientDashboard RPC hook.
    Verify client activation functionality (AUTH-03) is preserved.
    Verify both translation JSON files have matching dashboard/history keys.
    Verify `src/App.tsx` has /client/history route.
  </verify>
  <done>
    Client can view workout history as a date-sorted list showing session details (date, day, duration, sets). Trainer dashboard shows a searchable client list with activity status and color-coded today's workout indicators (green=done, yellow=in progress, gray=not started). Trainer can tap any client to see their plan and workout history. Client activation still works. All text translated.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 2 end-to-end flow</name>
  <files>none (verification only)</files>
  <action>
    Human verifies the complete Phase 2 end-to-end flow. What was built:
    Complete Phase 2: Training Plan Builder (create plans with days, exercises, sets/reps/weight, cycle length, versioning), Client Workout Flow (view plan by day, YouTube exercise demos, gym-floor set logging with pre-fill and steppers, session timer, summary), Workout History (past sessions with details), Trainer Dashboard (client list with search, color-coded status, drill-in to client details).

    Prerequisites: Phase 1 is complete, Supabase project is configured, Phase 2 migration (00002_training_plans_and_workouts.sql) has been run in Supabase SQL Editor.

    Verification steps:

    **1. Plan Builder (PLAN-01, PLAN-02, PLAN-03, PLAN-05):**
    - Log in as trainer
    - Navigate to Clients, select a client (activate them first if needed)
    - Create a new training plan:
      - Set name "Programa Fuerza" and cycle length 4 weeks
      - Add Day A with 2-3 exercises (search for them in the picker)
      - Set prescriptions: 3 sets x 10 reps @ 60kg for each
      - Add Day B with 2-3 different exercises
      - Reorder exercises within a day using up/down arrows
      - Save the plan
    - Activate the plan
    - Confirm the plan appears in the client's plan view

    **2. Plan Versioning (PLAN-06):**
    - From the active plan view, click "Edit Plan"
    - Confirm a new draft version is created (Version 2)
    - Make a change (add an exercise or change reps)
    - Save and activate the new version
    - Confirm the old version is archived

    **3. Client Plan View (PLAN-04, EXER-03):**
    - Log in as the client (or use a different browser)
    - Navigate to My Plan
    - Confirm plan shows grouped by day tabs
    - Confirm the suggested next day is highlighted
    - Tap an exercise play icon -- YouTube video should load inline
    - Tap "Start Workout" for Day A

    **4. Workout Logging (WLOG-01, WLOG-02, WLOG-03):**
    - Confirm workout session starts (timer begins)
    - Confirm set values are pre-filled (first time: from prescribed values since no history)
    - Log each set: tap the log button (should require just 1 tap if values are correct)
    - Try adjusting weight with stepper buttons (+/- 2.5kg)
    - Try adjusting reps with stepper buttons (+/- 1)
    - Try typing a value with comma as decimal separator (e.g., "62,5")
    - Tap "Finish Workout"
    - Confirm summary shows duration, sets logged, total weight

    **5. Pre-fill from Last Session (LOCKED DECISION):**
    - Start another workout for the same day
    - Confirm set values are pre-filled from the previous session's ACTUAL logged values (not prescribed)

    **6. Session Resume:**
    - Start a workout, log 1-2 sets, then navigate away
    - Come back to the same day's workout
    - Confirm the session resumes with previously logged sets shown

    **7. Workout History (WLOG-04):**
    - Navigate to History (client side)
    - Confirm completed sessions appear with date, duration, and set count

    **8. Trainer Dashboard (DASH-01, DASH-02, DASH-03):**
    - Log in as trainer
    - Dashboard should show client list with:
      - Color-coded status: green (completed today), yellow (in progress), gray (not started)
      - Activity status (Active/Inactive/Pending)
      - Search: type part of client name, list filters
    - Tap a client to see their detail view with Plan and Workouts tabs
    - Confirm workout history shows in the Workouts tab

    **9. Mobile Responsiveness:**
    - Test all above flows at 375px width (iPhone SE)
    - Workout logging: steppers and log buttons should be easily tappable with one thumb
    - No horizontal scrolling on any page

    **10. i18n:**
    - Toggle language on any page
    - Confirm all Phase 2 text switches between Spanish and English

    Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>Human confirms all 10 verification areas pass by typing "approved".</verify>
  <done>All Phase 2 success criteria verified: plan builder, versioning, client plan view, workout logging with pre-fill, session resume, workout history, trainer dashboard with status indicators, mobile responsiveness, and i18n.</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes with zero errors
2. Client workout history shows past sessions with date, duration, and set details
3. Trainer dashboard loads client list from single RPC query (not N+1)
4. Search filters clients by name in real-time
5. Color-coded status indicators: green (completed), yellow (in progress), gray (not started)
6. Trainer can drill into client detail with plan and workout tabs
7. Client activation (AUTH-03) still works after dashboard upgrade
8. All text translated in both Spanish and English
9. Full Phase 2 flow verified by human checkpoint
</verification>

<success_criteria>
- Workout history displays all completed sessions with meaningful stats
- Dashboard uses single get_client_dashboard RPC call (not N+1 queries)
- Client search is responsive with debounce
- Status badges use green/yellow/gray color scheme consistently
- Client detail tabs show plan and workout data
- Client activation preserved from Phase 1
- Human verifies all 10 checkpoint areas pass
- All Phase 2 success criteria from ROADMAP.md are met
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-training-loop/02-04-SUMMARY.md`
</output>
