---
phase: 02-core-training-loop
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/features/workouts/components/workout-session.tsx
  - src/features/workouts/components/exercise-logger.tsx
  - src/features/workouts/components/set-row.tsx
  - src/features/workouts/components/youtube-player.tsx
  - src/features/workouts/components/session-summary.tsx
  - src/features/plans/components/client-plan-view.tsx
  - src/features/plans/components/training-day-tab.tsx
  - src/pages/client/MyPlanPage.tsx
  - src/pages/client/WorkoutPage.tsx
  - src/layouts/ClientLayout.tsx
  - src/App.tsx
  - src/locales/en/translation.json
  - src/locales/es/translation.json
autonomous: true
requirements:
  - PLAN-04
  - WLOG-01
  - WLOG-02
  - WLOG-03
  - EXER-03

must_haves:
  truths:
    - "Client can view their assigned training plan grouped by training day with tabs"
    - "Client can tap an exercise to see its YouTube video via react-lite-youtube-embed"
    - "Client can start a workout session for a training day"
    - "Client can log weight and reps for each set with stepper buttons and pre-filled values from last session"
    - "Workout session records start time and completion time"
  artifacts:
    - path: "src/features/workouts/components/workout-session.tsx"
      provides: "Active workout session container managing exercises and session lifecycle"
      min_lines: 50
    - path: "src/features/workouts/components/set-row.tsx"
      provides: "Single set input with weight/reps steppers and log button"
      min_lines: 40
    - path: "src/features/workouts/components/youtube-player.tsx"
      provides: "Wrapper around react-lite-youtube-embed for exercise videos"
      min_lines: 15
    - path: "src/features/plans/components/client-plan-view.tsx"
      provides: "Client's plan view with training day tabs"
      min_lines: 30
    - path: "src/pages/client/WorkoutPage.tsx"
      provides: "Workout logging page for a training day"
      min_lines: 30
  key_links:
    - from: "src/features/workouts/components/set-row.tsx"
      to: "src/features/workouts/hooks/useWorkoutMutations.ts"
      via: "useLogSet mutation for persisting each set"
      pattern: "useLogSet"
    - from: "src/features/workouts/components/workout-session.tsx"
      to: "src/features/workouts/hooks/useWorkouts.ts"
      via: "useLastSessionValues for pre-fill data"
      pattern: "useLastSessionValues"
    - from: "src/features/workouts/components/youtube-player.tsx"
      to: "react-lite-youtube-embed"
      via: "LiteYouTubeEmbed component import"
      pattern: "LiteYouTubeEmbed"
    - from: "src/pages/client/MyPlanPage.tsx"
      to: "src/features/plans/hooks/usePlans.ts"
      via: "useActivePlan to fetch client's plan"
      pattern: "useActivePlan"
---

<objective>
Build the client's training experience: view their plan by training day, watch exercise demonstration videos, start a workout session, and log every set with pre-filled values and stepper controls optimized for gym use.

Purpose: This is the core daily interaction for clients -- the product's reason to exist. The gym-floor UX must be fast, minimal-tap, and work with sweaty hands on a phone.
Output: Client plan view, YouTube exercise embed, workout session logging, session summary
</objective>

<execution_context>
@/Users/macbookpro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookpro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-training-loop/02-RESEARCH.md
@.planning/phases/02-core-training-loop/02-01-SUMMARY.md
@src/features/workouts/types.ts
@src/features/workouts/hooks/useWorkouts.ts
@src/features/workouts/hooks/useWorkoutMutations.ts
@src/features/plans/hooks/usePlans.ts
@src/lib/utils/youtube.ts
@src/components/DecimalInput.tsx
@src/App.tsx
@src/layouts/ClientLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client plan view with training day tabs and YouTube exercise embed</name>
  <files>
src/features/plans/components/client-plan-view.tsx
src/features/plans/components/training-day-tab.tsx
src/features/workouts/components/youtube-player.tsx
src/pages/client/MyPlanPage.tsx
  </files>
  <action>
**Create `src/features/workouts/components/youtube-player.tsx`:**
Wrapper around react-lite-youtube-embed for exercise demonstration videos.
- Props: youtubeUrl (string), exerciseName (string)
- Uses extractYouTubeVideoId() from src/lib/utils/youtube.ts to parse the URL
- If no valid video ID, return null
- Renders LiteYouTubeEmbed with: id={videoId}, title={exerciseName}, poster="hqdefault", noCookie={true}
- Import CSS: `import "react-lite-youtube-embed/dist/LiteYouTubeEmbed.css"`
- Wrap in a div with `className="w-full aspect-video rounded-lg overflow-hidden"`
- EXER-03 requirement: Client can view exercise with inline YouTube video embed

**Create `src/features/plans/components/training-day-tab.tsx`:**
Single training day content showing exercises within that day.
- Props: trainingDay (TrainingDay with nested plan_exercises and exercises), onStartWorkout (trainingDayId => void)
- Lists exercises with: name, prescribed sets x reps @ weight (formatted with locale-aware formatting)
- Each exercise row: tap to expand and show YouTube video (YouTubePlayer component) -- per discretion: video behind a tap, minimal distraction during logging
- "Start Workout" button at the bottom (large, 56px height, primary color) navigates to the workout page for this day

**Create `src/features/plans/components/client-plan-view.tsx`:**
Client's full plan view with tabs for each training day.
- Props: plan (PlanWithDays)
- Uses shadcn/ui Tabs component (add via `pnpm dlx shadcn@latest add tabs` if not already installed)
- Each tab = one training day (day_label as tab trigger)
- Per discretion: tabs chosen over cards because 3-6 training days fit well in a horizontal tab bar on mobile
- Tab content renders TrainingDayTab for the selected day
- Shows plan name and version at the top

**Create `src/pages/client/MyPlanPage.tsx`:**
Page for the client to view their assigned training plan.
- Route: /client/plan
- Uses useAuth() to get current user ID
- Uses useActivePlan(userId) to fetch the client's active plan
- Loading state: skeleton cards
- No plan state: friendly message "No training plan assigned yet. Your trainer will create one for you."
- Plan exists: render ClientPlanView with the plan data
- "Start Workout" button in each day tab navigates to /client/workout/:trainingDayId
  </action>
  <verify>
Run `pnpm build` -- must pass with zero TypeScript errors. Verify: (1) youtube-player.tsx imports from react-lite-youtube-embed, (2) client-plan-view.tsx uses Tabs component, (3) MyPlanPage.tsx uses useActivePlan hook.
  </verify>
  <done>Client can navigate to their plan page, see training days as tabs, view exercises within each day with prescribed values, tap an exercise to see its YouTube video, and tap "Start Workout" to begin logging for a specific day.</done>
</task>

<task type="auto">
  <name>Task 2: Create workout session logging with pre-fill, stepper controls, and session lifecycle</name>
  <files>
src/features/workouts/components/workout-session.tsx
src/features/workouts/components/exercise-logger.tsx
src/features/workouts/components/set-row.tsx
src/features/workouts/components/session-summary.tsx
src/pages/client/WorkoutPage.tsx
src/layouts/ClientLayout.tsx
src/App.tsx
src/locales/en/translation.json
src/locales/es/translation.json
  </files>
  <action>
**Create `src/features/workouts/components/set-row.tsx`:**
Single set input row with stepper controls (from 02-RESEARCH.md Pattern 3).
- Props: setNumber, prefillWeight, prefillReps, onLog callback, isLogged (boolean)
- Pre-fill priority (LOCKED DECISION): last session actual values first, falls back to prescribed values
- Weight stepper: -2.5 / value / +2.5 buttons (each 48px, using inputMode="decimal" for the value input)
- Reps stepper: -1 / value / +1 buttons (each 48px, using inputMode="numeric" for the value input)
- Log button (48px circle): shows Save icon when unlogged, Check icon with green background when logged
- Use normalizeDecimal() from src/lib/utils.ts for weight input parsing (comma/period support per INFR-03)
- Minimum height 56px per row for gym-friendly touch targets
- Disabled state after logging (green check, no further edits for this set in this session)

**Create `src/features/workouts/components/exercise-logger.tsx`:**
Per-exercise logging interface showing all sets for one exercise.
- Props: planExercise (with exercise details), prefillSets (from last session), sessionId, exerciseIndex, totalExercises
- Shows exercise name and a small video icon button that expands to show YouTubePlayer (behind tap, per discretion)
- Renders SetRow for each prescribed set (using prescribed_sets count)
- Pre-fills each set from the prefillSets array (from useLastSessionValues), falling back to prescribed values
- Shows progress indicator: "Set 2/4 logged"
- Per discretion: show one exercise at a time (focused view) for gym UX -- use an accordion-style expand or a "current exercise" stepper. Choose accordion so the client can scroll back to review previous exercises.

**Create `src/features/workouts/components/workout-session.tsx`:**
Container component managing the active workout session.
- Props: trainingDayId (string), trainingDay (TrainingDay with exercises)
- On mount: calls useStartSession() mutation to create a new workout_session (unless resuming -- per discretion, always start fresh for v1 simplicity)
- Fetches pre-fill data via useLastSessionValues(clientId, trainingDayId)
- Renders ExerciseLogger for each exercise in the training day
- "Finish Workout" button at the bottom (large, 56px, primary)
- On finish: calls useCompleteSession() mutation to set completed_at
- After completion: shows SessionSummary
- Session timer: display elapsed time since started_at (update every second via setInterval)

**Create `src/features/workouts/components/session-summary.tsx`:**
Post-workout summary shown after completing a session.
- Per discretion: show a simple stats screen (adds value without being annoying)
- Shows: session duration, total sets logged, total exercises completed
- "Done" button navigates back to the plan page
- Clean, celebratory-but-simple design (checkmark icon, green accent)

**Create `src/pages/client/WorkoutPage.tsx`:**
Page for active workout logging.
- Route: /client/workout/:trainingDayId
- Fetches the training day data (with exercises) from the active plan
- Renders WorkoutSession component with the training day data
- Back button with confirmation dialog if session is in progress ("Leave workout? Your logged sets are saved.")
- Full-screen layout (no bottom nav during workout for maximum screen space)

**Update `src/layouts/ClientLayout.tsx`:**
- Update bottom navigation: add "My Plan" nav item (ClipboardList icon from lucide-react)
- Nav items: Home, My Plan, History (plan for history in Plan 04)
- Path: /client -> Home, /client/plan -> My Plan

**Update `src/App.tsx`:**
Add new client routes inside the existing client ProtectedRoute:
- /client/plan -> MyPlanPage
- /client/workout/:trainingDayId -> WorkoutPage (consider putting this outside ClientLayout since workout page should be full-screen without bottom nav)

**Update translation files (`src/locales/en/translation.json` and `src/locales/es/translation.json`):**
Add all Phase 2 workout-related translation keys:
- workout.startWorkout, workout.finishWorkout, workout.set, workout.sets, workout.reps, workout.weight, workout.kg, workout.logged, workout.sessionTimer, workout.leaveConfirm, workout.setsLogged, workout.exercisesCompleted, workout.duration, workout.done, workout.noSetsYet
- plan.myPlan, plan.noPlan, plan.noPlanDescription, plan.trainingDay, plan.showVideo, plan.hideVideo
- nav.myPlan, nav.history
  </action>
  <verify>
Run `pnpm build` -- must pass with zero TypeScript errors. Verify: (1) set-row.tsx uses stepper buttons with 48px+ touch targets, (2) workout-session.tsx calls useStartSession and useCompleteSession, (3) exercise-logger.tsx renders SetRow for each prescribed set, (4) Routes /client/plan and /client/workout/:trainingDayId registered in App.tsx. Check that all translation keys are present in both EN and ES files.
  </verify>
  <done>Client can view their plan by training day, start a workout session, see pre-filled values from their last session, adjust weight/reps via stepper buttons, log each set with a single tap, see a session timer, finish the workout, and see a summary screen. All interactions use large touch targets optimized for gym use.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero errors
- Client routes registered: /client/plan, /client/workout/:trainingDayId
- YouTubePlayer uses react-lite-youtube-embed (not raw iframe) per EXER-03
- Set logging pre-fills from last session actual values (not prescribed) per locked decision
- Stepper buttons are 48px+ touch targets per gym UX requirement
- Session timer tracks start and completion time (WLOG-03)
- All visible text uses t() translations in both Spanish and English
</verification>

<success_criteria>
- Client can view their training plan organized by training days (PLAN-04)
- Client can tap an exercise to watch its YouTube video inline (EXER-03)
- Client can start a workout session for a training day (WLOG-01)
- Client can log weight and reps for each set with pre-filled values and stepper controls (WLOG-02)
- Workout session tracks start and completion time (WLOG-03)
- Set logging uses optimistic updates (appears immediately, syncs in background)
- Gym-floor UX: minimal taps, large touch targets, one-exercise-at-a-time focus
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-training-loop/02-03-SUMMARY.md`
</output>
