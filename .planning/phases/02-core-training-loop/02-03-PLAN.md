---
phase: 02-core-training-loop
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/features/workouts/components/workout-session.tsx
  - src/features/workouts/components/exercise-logger.tsx
  - src/features/workouts/components/set-row.tsx
  - src/features/workouts/components/youtube-player.tsx
  - src/features/workouts/components/session-summary.tsx
  - src/pages/client/my-plan.tsx
  - src/pages/client/workout.tsx
  - src/layouts/ClientLayout.tsx
  - src/App.tsx
  - public/locales/en/translation.json
  - public/locales/es/translation.json
autonomous: true
requirements:
  - PLAN-04
  - EXER-03
  - WLOG-01
  - WLOG-02
  - WLOG-03

must_haves:
  truths:
    - "Client can view their assigned training plan grouped by training day with tabs"
    - "Client can tap an exercise to watch its YouTube video inline via lightweight embed"
    - "Client can start a workout session for any training day"
    - "Workout set inputs are pre-filled from last session's actual values (not prescribed)"
    - "Client can log weight and reps for each set with stepper buttons and minimal taps"
    - "Workout session tracks start time and completion time"
    - "Incomplete sessions from today are automatically resumed"
    - "All workout UI is fully translated in Spanish and English"
    - "Workout logging is usable with one thumb on a mobile phone"
  artifacts:
    - path: "src/pages/client/my-plan.tsx"
      provides: "Client plan view with training day tabs"
      contains: "MyPlanPage"
    - path: "src/features/workouts/components/workout-session.tsx"
      provides: "Active workout session container"
      contains: "WorkoutSession"
    - path: "src/features/workouts/components/set-row.tsx"
      provides: "Set logging row with stepper buttons"
      contains: "SetRow"
    - path: "src/features/workouts/components/youtube-player.tsx"
      provides: "Lightweight YouTube embed wrapper"
      contains: "YouTubePlayer"
  key_links:
    - from: "src/pages/client/my-plan.tsx"
      to: "src/features/plans/hooks/usePlans.ts"
      via: "useActivePlan for client's own plan"
      pattern: "useActivePlan"
    - from: "src/features/workouts/components/workout-session.tsx"
      to: "src/features/workouts/hooks/useWorkouts.ts"
      via: "useLastSessionValues for pre-fill"
      pattern: "useLastSessionValues"
    - from: "src/features/workouts/components/set-row.tsx"
      to: "src/features/workouts/hooks/useWorkoutMutations.ts"
      via: "useLogSet with optimistic update"
      pattern: "useLogSet"
    - from: "src/features/workouts/components/youtube-player.tsx"
      to: "react-lite-youtube-embed"
      via: "LiteYouTubeEmbed component"
      pattern: "LiteYouTubeEmbed"
---

<objective>
Build the complete client workout flow: view training plan grouped by day, watch exercise videos inline, start a workout session, and log every set with a mobile-optimized gym-floor UI featuring pre-filled values, stepper buttons, and minimal taps.

Purpose: This is the product's core daily loop -- the client opens the app at the gym, sees their workout for the day, watches exercise demos if needed, and logs every set with minimal friction. This must work flawlessly with sweaty hands on a phone in one hand.

Output: Client plan view page with day tabs, workout session page with set logging, YouTube video embed, session summary, connected to routes in App.tsx.
</objective>

<execution_context>
@/Users/macbookpro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookpro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-training-loop/02-RESEARCH.md
@.planning/phases/02-core-training-loop/02-CONTEXT.md
@.planning/phases/02-core-training-loop/02-01-SUMMARY.md
@.planning/phases/02-core-training-loop/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build client plan view with day tabs and YouTube exercise embed</name>
  <files>
    src/features/workouts/components/youtube-player.tsx
    src/pages/client/my-plan.tsx
    src/pages/client/ClientHome.tsx
  </files>
  <action>
    **Claude's Discretion decisions (from CONTEXT.md):**
    - Day layout: tabs (works well for 3-6 training days, familiar pattern, compact on mobile)
    - Next day suggestion: highlight the suggested next day tab based on last completed workout
    - YouTube video access: behind a tap icon (play button) -- minimal distraction during workout, client taps only when they need to see the demo

    1. Create `src/features/workouts/components/youtube-player.tsx`:
       - Wrapper around `react-lite-youtube-embed` (LiteYouTubeEmbed)
       - Props: `youtubeUrl: string`, `exerciseName: string`
       - Uses `extractYouTubeVideoId` from `src/lib/utils/youtube.ts` to parse the URL
       - If no valid video ID, renders nothing (graceful fallback)
       - Import `react-lite-youtube-embed/dist/LiteYouTubeEmbed.css` for styles
       - Set `noCookie={true}` for privacy (youtube-nocookie.com)
       - Set `poster="hqdefault"` for good thumbnail quality
       - Wrapped in `aspect-video rounded-lg overflow-hidden` container
       - See RESEARCH.md Pattern 4

    2. Create `src/pages/client/my-plan.tsx`:
       - Route: `/client/plan`
       - Uses `useAuth()` to get current user ID
       - Uses `useActivePlan(userId)` to fetch the client's active training plan
       - **Day Navigation:** Use tabs (one tab per training day). Use shadcn/ui-style tab buttons (styled div buttons, not shadcn Tabs component since it may need to be added). Horizontally scrollable if > 4 days.
       - **Next Day Suggestion:** Highlight the suggested next day by checking which day was last completed. Use a simple heuristic: query last workout session's training_day_id, find its day_order, suggest (day_order + 1) % total_days. Default to first day if no history.
       - **Day Content:** For each exercise in the selected day, show:
         - Exercise name (bold)
         - Prescription summary: "{sets} x {reps} @ {weight}kg"
         - Play icon button (lucide Play) that expands to show YouTubePlayer inline when tapped. Uses a local `expandedExerciseId` state. This keeps the view clean until the client needs the video.
       - **Start Workout Button:** Prominent button at bottom of day view: "Start Workout" (translated). Links to `/client/workout/:trainingDayId`.
       - **No Plan State:** If no active plan, show friendly message: "No training plan assigned yet. Ask your trainer to create one." with a dumbbell icon.
       - Loading state, error state with retry
       - All text translated

    3. Update `src/pages/client/ClientHome.tsx`:
       - Replace placeholder content with a proper home page that shows:
         - Greeting: "Hello, {name}" (translated)
         - Quick action card: "Today's Workout" linking to my-plan page
         - If there's an active plan, show the suggested next training day name
         - If there's an incomplete session today, show "Resume Workout" instead
       - Use useActivePlan and useTodaySession hooks for data
       - Keep it simple and focused -- this is the first screen the client sees
  </action>
  <verify>
    Verify `pnpm build` passes with zero TypeScript errors.
    Verify `src/features/workouts/components/youtube-player.tsx` imports LiteYouTubeEmbed and extractYouTubeVideoId.
    Verify `src/pages/client/my-plan.tsx` uses useActivePlan and renders day tabs.
    Verify YouTube player uses noCookie={true}.
    Verify play icon expands to show video (not always visible -- per discretion decision).
  </verify>
  <done>
    Client can view their training plan grouped by day tabs with the next suggested day highlighted. Each exercise shows prescription details and a play icon to expand inline YouTube video. "Start Workout" button links to the workout session. Client home page shows quick access to today's workout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build workout session logging with pre-fill, steppers, and session lifecycle</name>
  <files>
    src/features/workouts/components/workout-session.tsx
    src/features/workouts/components/exercise-logger.tsx
    src/features/workouts/components/set-row.tsx
    src/features/workouts/components/session-summary.tsx
    src/pages/client/workout.tsx
    src/layouts/ClientLayout.tsx
    src/App.tsx
    public/locales/en/translation.json
    public/locales/es/translation.json
  </files>
  <action>
    **Claude's Discretion decisions (from CONTEXT.md):**
    - Exercise display: show one exercise at a time (focused mode) with accordion-style expand. This minimizes visual clutter and helps the client focus on their current exercise at the gym.
    - Post-workout summary: show a stats screen with total sets logged, total weight lifted, session duration. Quick and informative without being annoying.
    - Session resume: if incomplete session exists for today, resume it (show previously logged sets, allow continuing). This handles the common case of closing the browser mid-workout.

    1. Create `src/features/workouts/components/set-row.tsx`:
       - THE most critical mobile UX component. See RESEARCH.md Pattern 3 (Gym-Floor Workout Logging UI).
       - Props: `setNumber: number`, `defaultWeight: number`, `defaultReps: number`, `isLogged: boolean`, `onLog: (data: { set_number: number; weight_kg: number; reps: number }) => void`
       - Pre-fill weight and reps from props (which come from last session actual values per locked decision, falling back to prescribed)
       - **Weight input:** Stepper buttons (-2.5 / +2.5 kg) flanking a center value display. Buttons are 48px minimum (h-12 w-12). Value display is tappable to open keyboard input (inputMode="decimal", accepts comma/period via normalizeDecimal from src/lib/utils.ts).
       - **Reps input:** Stepper buttons (-1 / +1) flanking a center value. Same sizing. inputMode="numeric" when tapped.
       - **Log button:** 48px (h-12 w-12) button. Shows Save icon when unlogged, turns green with Check icon when logged.
       - **One-tap "as prescribed" not needed** since we pre-fill from last session -- the default values are already what the client usually does. They just tap the log button if values are correct.
       - Min-height per row: 56px for easy thumb targeting
       - Use `useState` for local weight/reps state (not form library -- each set is independently logged)
       - After logging, disable the log button (set shows as complete with green check)

    2. Create `src/features/workouts/components/exercise-logger.tsx`:
       - Props: `planExercise: PlanExercise` (with exercise details), `lastSessionSets: { weight_kg: number; reps: number }[] | undefined`, `sessionId: string`, `loggedSets: WorkoutSet[]`
       - Shows exercise name in header, small play icon for video
       - Generates SetRow components for each prescribed set (1 to prescribed_sets)
       - Pre-fill logic (LOCKED DECISION): For each set, use lastSessionSets[setIndex] if available, else fall back to prescribed values (prescribed_weight_kg, prescribed_reps)
       - Uses `useLogSet(sessionId)` mutation for each set log (with optimistic update)
       - Shows which sets are already logged (from loggedSets prop -- for session resume)
       - Accordion/collapsible: shows exercise name + progress (e.g., "2/3 sets") when collapsed, full set rows when expanded
       - YouTube video accessible via small Play icon that opens the YouTubePlayer inline below the exercise header

    3. Create `src/features/workouts/components/workout-session.tsx`:
       - Props: `trainingDayId: string`, `session: WorkoutSession`, `planDay: TrainingDay with exercises`
       - Container component orchestrating the workout
       - Fetches last session values with `useLastSessionValues(clientId, trainingDayId)`
       - Fetches current session's logged sets with `useWorkoutSession(session.id)`
       - Shows session timer: elapsed time since started_at (updates every minute using setInterval)
       - Renders ExerciseLogger for each exercise in the day
       - "Finish Workout" button at bottom (prominent, full-width, h-14 for thumb reach)
       - Completing calls `useCompleteSession()` mutation
       - After completion, shows SessionSummary

    4. Create `src/features/workouts/components/session-summary.tsx`:
       - Props: `session: WorkoutSessionWithSets`, `duration: string`
       - Post-workout stats screen:
         - Session duration (formatted with formatSessionDuration from cycle.ts)
         - Total sets logged (count)
         - Total weight lifted (sum of weight_kg * reps across all sets, formatted)
         - Exercises completed (count of exercises with at least 1 logged set)
       - "Back to Plan" button linking to /client/plan
       - Celebratory but not annoying -- simple card with stats, checkmark icon
       - All text translated

    5. Create `src/pages/client/workout.tsx`:
       - Route: `/client/workout/:trainingDayId`
       - Uses `useAuth()` to get client ID
       - Uses `useTodaySession(clientId, trainingDayId)` to check for existing session
       - **Session resume (discretion decision):** If incomplete session exists for today, resume it (pass existing session to WorkoutSession). If no session, auto-start one using `useStartSession()` mutation on mount.
       - Uses `useActivePlan(clientId)` to get the plan day data (exercises, prescriptions)
       - Finds the specific training day from the plan by trainingDayId
       - Shows day label in page header
       - Back button to /client/plan
       - Loading state while session starts
       - Error state if plan not found or day not found

    6. Update `src/layouts/ClientLayout.tsx`:
       - Update nav items: Home (/client), My Plan (/client/plan), History (/client/history -- placeholder route for Plan 04)
       - Use ClipboardList icon for "My Plan", History icon for "History"
       - Keep Home icon for home

    7. Update `src/App.tsx`:
       - Add client routes inside existing client ProtectedRoute + ClientLayout:
         - `/client/plan` -> MyPlanPage
         - `/client/workout/:trainingDayId` -> WorkoutPage
         - `/client/history` -> placeholder (will be built in Plan 04)
       - Keep existing `/client` (ClientHome) route

    8. Update translation files (both en and es):
       - Plan view: "My Training Plan", "No plan assigned", "Ask your trainer to create a plan", "Start Workout", "Resume Workout", "Today's Workout", "Suggested"
       - Workout: "Workout", "Set", "Weight", "Reps", "Log", "Logged", "Finish Workout", "Session Timer", "sets completed"
       - Exercise: "Watch Video", "Hide Video"
       - Summary: "Workout Complete!", "Duration", "Sets Logged", "Total Weight", "Exercises", "Back to Plan"
       - Client home: "Hello, {name}", "Today's Workout", "Resume your workout", "View your plan"
       - Navigation: "My Plan", "History"
       - Error/loading: "Loading workout...", "Starting session...", "Error starting session"
       - Ensure ALL keys exist in BOTH en and es files
  </action>
  <verify>
    Verify `pnpm build` passes with zero TypeScript errors.
    Verify `src/features/workouts/components/set-row.tsx` has stepper buttons (h-12 w-12), inputMode="decimal" for weight, inputMode="numeric" for reps.
    Verify `src/features/workouts/components/exercise-logger.tsx` pre-fills from lastSessionSets (not prescribed values directly).
    Verify `src/pages/client/workout.tsx` checks for existing session (session resume behavior).
    Verify `src/features/workouts/components/workout-session.tsx` uses useLastSessionValues.
    Verify `src/App.tsx` has routes for /client/plan, /client/workout/:trainingDayId, /client/history.
    Verify `src/layouts/ClientLayout.tsx` has 3 nav items (Home, My Plan, History).
    Verify both translation JSON files have matching workout-related keys.
  </verify>
  <done>
    Client can view their plan by day tabs, tap exercises to watch YouTube demos, start a workout session that pre-fills from last session's actual values, log each set with stepper buttons (+/- weight and reps) requiring just 1 tap when values are correct, see a session timer, and finish with a stats summary. Incomplete sessions resume automatically. All workout UI is optimized for one-thumb mobile operation with 48px+ touch targets.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` completes with zero errors
2. Client can view plan grouped by training day tabs
3. YouTube videos load as lightweight embeds (not full iframes)
4. Set values pre-filled from last session actual values (LOCKED decision verified)
5. Stepper buttons work for weight (+/- 2.5kg) and reps (+/- 1)
6. Set logged immediately on tap (optimistic update)
7. Session timer tracks elapsed time
8. Finish Workout shows summary with duration and stats
9. Incomplete sessions resume on return
10. All text translated in Spanish and English
11. All touch targets >= 48px, workout page usable at 375px width
</verification>

<success_criteria>
- Client plan view loads active plan and groups by day with tabs
- YouTube embed uses react-lite-youtube-embed (not raw iframe), loads thumbnail first
- Pre-fill uses last session actual values, falling back to prescribed (locked decision)
- Set logging uses optimistic updates (appears logged immediately before server confirms)
- Weight input accepts both comma and period (INFR-03)
- Session auto-starts when workout page opens, auto-resumes if incomplete today
- Session duration computed from started_at to completed_at
- Mobile-first: one-thumb operation, large steppers, minimal taps per set
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-training-loop/02-03-SUMMARY.md`
</output>
